import { AlignJustify, X, XCircle, XSquare } from "lucide-react";

const MobileNavigation = () => {
  return (
    <div className="sm:hidden flex flex-1 justify-end items-center">
      MobileNavigation
    </div>
  );
};

export default MobileNavigation;
